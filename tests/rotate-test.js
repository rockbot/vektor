var Vector = require('../').vector;
var Rotate = require('../').rotate;
var should = require('should');

describe('Rotating a point', function () {
  it('should rotate it around the z-axis', function (done) {
    done();
  });
});